name: aqi
version: "1.0.0"
profile: aqi
model-paths: ["models"]
test-paths: ["tests"]
models:
  aqi:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
